# service.yaml
apiVersion: v1
kind: Service
metadata:
  name: prod-service
spec:
  selector:
    app: prod-service
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
  type: {{ .Values.service.type }}
